---
feature: member-partner-distinction
complexity: standard
generated_by: clarify
generated_at: 2026-07-18T00:00:00+08:00
version: 1
---

# 需求文档: 区分「联盟成员伙伴」与「工作组伙伴」

## 1. 概述

**一句话描述**：在术语、页面呈现与导航上，明确区分官网已有的两类合作单位身份——「联盟成员单位」与「工作组共建单位」，避免访客混淆。

- **核心价值**：让访客一眼分清「某单位是联盟正式会员」还是「仅为某工作组的共建单位」，并讲清两者可能重叠的关系，契合仓库既有治理边界（未授权不具名）。
- **目标用户**：官网访客（潜在成员单位、合作方、媒体、公众）。

> 说明：本需求**不新增**「合作伙伴 / 生态伙伴」第三类内容模型，仅围绕已有的 `MemberSummary`（联盟成员）与 `WorkingGroupMember`（工作组成员）两类身份做区分与呈现。

## 2. 需求与用户故事

### R1 术语区分
作为访客，我希望两类页面的标题/描述能清楚表明身份类型，以便区分联盟会员与工作组共建单位。
- **验收**：`/members` 页明确呈现为「联盟成员伙伴」；工作组成员页（`/working-groups/[slug]/members`）明确呈现为「工作组共建伙伴/成员」；zh 与 en 两套 STRINGS 同步更新。

### R2 两者关系互链
作为访客，我希望在两类页面间看到彼此关系的说明与链接，以便理解身份可能重叠。
- **验收**：成员页注明「部分成员亦参与工作组共建」并链接至工作组；工作组成员页注明「名单单位多为联盟会员」并链接至成员页；文案 zh/en 同步。

### R3 导航二级分组
作为访客，我希望从顶栏导航能分别进入两类成员入口。
- **验收**：顶栏「成员伙伴」改为**二级分组**，含「联盟成员」与「工作组成员」两个子项；`NavigationItem` 类型扩展为支持子项；站点 header 桌面端与移动端菜单均能正确渲染下拉/分组。

### R4 首页体现
作为访客，我希望首页也能感知到两类身份的入口/区分。
- **验收**：首页（`home-view.tsx`）相关区块体现两类入口或说明，与顶栏分组语义一致。

## 3. 功能验收清单

- [ ] `/members` 页标题/hero 明确为「联盟成员伙伴」（zh + en）
- [ ] 工作组成员页明确为「工作组共建伙伴/成员」（zh + en）
- [ ] 两类页面间存在关系说明文案 + 互链（zh + en）
- [ ] 顶栏「成员伙伴」呈现为二级分组，含「联盟成员」「工作组成员」子项
- [ ] `NavigationItem` 类型支持子项，header 桌面端 + 移动端均正确渲染
- [ ] 首页体现两类入口/区分，与导航语义一致
- [ ] `pnpm typecheck && pnpm lint && pnpm test && pnpm build` 全通过

## 4. 技术约束

- **不改数据结构**：保持 `MemberSummary`、`WorkingGroupMember` 现有字段不变；仅动展示层与导航配置。
- **双语一致**：所有文案改动须同步 `zh` 与 `en` 两套 STRINGS。
- **导航类型扩展**：`src/types/content.ts` 的 `NavigationItem` 需新增可选 `children`（子项），并保持向后兼容现有扁平项。
- **改动点（集成点）**：
  - `src/config/site.ts`（`SITE_NAVIGATION`）
  - `src/types/content.ts`（`NavigationItem`）
  - 站点 header / 移动端导航组件（渲染二级分组）
  - `src/components/pages/members-view.tsx`（术语 + 互链文案）
  - `src/app/(frontend)/working-groups/[slug]/members/page.tsx`（术语 + 互链文案）
  - `src/components/pages/home-view.tsx`（首页入口/说明）
- **治理边界**：沿用现有空态占位策略，未授权单位不具名、不加品牌标识。

## 5. 排除项

- 不新增「合作伙伴 / 生态伙伴」等第三类**非会员**内容模型。
- 不改动成员/工作组成员的**数据来源与名单内容**（不新增、不填充具名单位）。
- 不涉及后端、表单、飞书接入等（纯静态展示）。

## 6. 风险与依赖

| 项 | 说明 |
|----|------|
| 风险：导航组件改造 | 二级分组需同时兼顾桌面下拉与移动端展开，注意可访问性（键盘/焦点）与激活态高亮（`PUBLIC_STATIC_ROUTES`/active 判定）。 |
| 风险：术语一致性 | 「成员 / 伙伴 / 共建单位」措辞需全站统一，避免新旧文案混用。 |
| 依赖 | 无外部依赖；均为仓库内类型化配置与组件改动。 |

## 7. 下一步

- 复杂度：**standard** → 建议进入 SDD 设计阶段细化导航组件方案与最终措辞。
- 后续命令：`/devagent:dev-spec-dev member-partner-distinction --skip-requirements`
