---
feature: reintroduce-pr29-quality
complexity: standard
generated_by: clarify
generated_at: 2026-07-19
version: 1
---

# 需求文档: 选择性重新引入 PR 29 的通用质量改进（第一档）

## 1. 概述

- **一句话描述**：在已回退 PR 29 的分支基础上，只把 PR 29 中**与视觉审美无关、零外部事实断言**的通用质量改进（无障碍、交互健壮性、json-ld 安全、免责/边界文案、去误导信息架构）外科式重新引入。
- **核心价值**：站长否决的是 PR 29 的视觉观感与信息架构风格；但其中夹带的可访问性修复、菜单焦点管理、防注入、隐私/免责边界等属于通用质量，与观感无关，丢弃可惜。本任务只捞这部分，不碰观感、不引入任何未核验的具名事实。
- **目标用户**：仓库维护者（m0xiaoxi）与站点最终访客（尤其依赖辅助技术者）。

## 2. 需求与用户故事

### R1 无障碍 / 交互健壮性（chrome 层）
作为使用键盘/屏幕阅读器的访客，我希望站点导航与菜单可达、可操作。
- 引入项（从 PR-29 逐组件提取，剥离视觉类名/图标改动）：
  - `site-header.tsx` 移动菜单焦点管理：`aria-expanded`/`aria-controls`、Escape 关闭并回焦触发器、外点关闭、`focusin` 移出关闭、桌面断点自动关闭、路由变化重置。
  - `language-toggle.tsx`：`aria-label` 含目标语言、两语 span 加 `lang`/`aria-hidden`。
  - `site-navigation-link.tsx` / `nav-group.tsx`：`showCurrent` 修复同页重复 `aria-current="page"`。
  - `site-footer.tsx`：`<nav aria-label>` + `<h2>` + `<ul>` 语义化（替代裸 `<div><br>`）。
  - `not-found.tsx` / `global-not-found.tsx`：`<main tabIndex={-1}>` + skip link 可聚焦目标。
  - `external-application-link.tsx`：保留 `rel="noreferrer noopener"` + `target="_blank"`，新增新窗口/外部服务可听提示与不可用态 `aria-live`。
- 验收：上述行为存在且不引入 PR-29 的视觉类名重构；对应单测覆盖（焦点管理、aria 属性、重复 current 修复）。

### R2 json-ld 注入防护
作为维护者，我希望结构化数据输出不被内容里的 `</script>` 破坏或注入。
- 引入：`json-ld.tsx` 的 `serializeJsonLd`（转义 `<` `>` `&` 及行分隔符）。
- 验收：含特殊字符的内容序列化后安全；单测覆盖转义。

### R3 免责 / 边界文案（自我声明，零外部事实断言）
作为维护者，我希望页面清晰声明本站与外部表单的处理边界，避免误导。
- 引入项：
  - **隐私页整体**（新增文件）：本站不接收/落库申请信息、无站内表单/接口/数据库、提交前核对目标表单信息处理说明。
  - `join` 页免责：区分"联盟入盟"vs"工作组议题参与"两路径、"提交不代表接纳/会员资格""请勿提交密钥/敏感数据"。
  - 各页免责短句："不据此推定机构标识/官方英文名/合作关系""是否启动、由谁参与、输出均需另行确认"。
  - 成员页空态文案、fail-closed 表单说明。
  - 网络安全页治理边界可执行化：合法来源+必要授权+明确范围、不公开密钥/个人信息、公开前相关方确认与合规审查。
- 验收：以上文案不含任何具名主体/合作关系/成果/官方英文名/上线声明等外部事实断言（逐句自查）。

### R4 去误导信息架构（结构，非风格化扩写）
作为访客，我希望参与路径清晰、机制表述克制不夸大。
- 引入项：
  - 首页 `PARTICIPATION_PATHS`：以指向 /join、/working-groups、/members、/news 的可操作路径替代口号（**前提：这些页面在回退后的分支上真实存在，实施时校验**）。
  - alliance 页 `MECHANISM` 四步结构（形成议题/组织参与/专题推进/确认公开）+ `OVERVIEW`"以经确认的信息为准"。
- 验收：新增内部链接目标页面存在（`pnpm build:export` 无死链/构建错误）；文案不恢复被删的误导性断言。

## 3. 功能验收清单

- [ ] `pnpm typecheck` 通过
- [ ] `pnpm lint` 通过
- [ ] `pnpm test` 通过（含新增/恢复的无障碍与 json-ld 单测）
- [ ] `pnpm build` 与 `pnpm build:export` 均成功、无死链
- [ ] 逐句自查：新增/引入文案零外部事实断言（无具名主体/合作方/成果/官方英文名/上线声明）
- [ ] 视觉观感未变（未引入 PR-29 的类名重构、图标、styles.css 观感部分）
- [ ] 人工键盘 + 屏幕阅读器验证移动菜单/语言切换/skip link：标注 Not run 或实际执行

## 4. 技术约束

- **外科式提取，禁止整文件 checkout**：第一档改进与 PR-29 的视觉类名、事实断言混在同一批文件里。必须逐组件/逐字段挑，`git show a0ce815:<file>` 作参考，手动移植行为逻辑与 aria/文案，**不** `git checkout a0ce815 -- <file>`。
- **视觉隔离**：只移植行为与语义（aria、焦点、rel、免责文案），不移植 PR-29 的 `styles.css` 观感改动与类名重构；若某无障碍行为依赖特定类名，用当前分支已有类名适配。
- **事实红线**：R3/R4 引入的所有文案必须是本站自我声明或中立结构，**不得**引入 PR-29 里被标记"需核验"的第二档断言（新闻来源、成员分组/职务/32 家、飞书运营方、具名安全合作方）。
- **链接完整性**：R4 的内部链接目标（/join、/working-groups、/members、/news）须在回退后分支真实存在，实施时逐一校验。
- **落地方式**：在 `revert/pr29-pages` 之上或新分支 `feat/reintroduce-a11y-quality`，独立 commit(s)，PR 评审后合入。不直接改 main、不自动合并、不触发正式发布。
- **测试**：无障碍行为与 json-ld 转义须有单测；沿用当前分支（pre-29）的测试框架与覆盖率阈值，不引入 PR-29 的高阈值/release-e2e 矩阵。

## 5. 排除项

- **不**引入任何视觉风格改动（类名重构、图标、styles.css 观感部分）。
- **不**引入第二档任何依赖外部事实核验的内容（新闻来源事实、成员分组/职务/总数、飞书运营方、具名安全合作方及官方英文名）。
- **不**恢复 PR-29 删除的误导性断言（主管部门指导组建、国际竞争力生态、官网正式上线声明）。
- **不**引入 facts 元数据体系（FactReference 等）——它服务第二档事实追踪，本任务不涉事实断言。
- **不**直接改 main、不合并、不改 DNS、不做正式生产发布。

## 6. 风险与依赖

| 风险/依赖 | 说明 | 应对 |
| --- | --- | --- |
| 无障碍行为与视觉类名耦合 | PR-29 焦点管理可能依赖其新类名 | 用当前分支已有类名适配，只移植行为逻辑 |
| 免责文案夹带隐性事实断言 | 边界文案里可能混入具名/外部主张 | 逐句自查，只保留自我声明与中立结构 |
| R4 内部链接指向不存在页面 | 回退后 IA 与 PR-29 不同 | 实施时校验目标页存在，build 验证无死链 |
| 无障碍正确性需人工验证 | 自动测试无法完全覆盖屏幕阅读器体验 | 单测覆盖 aria/焦点；人工键盘+SR 验证如未做须标 Not run |
| 依赖 revert PR（#30）先落地 | 本任务叠加在回退基础上 | 以 #30 合并后的 main 为基，或基于 revert 分支开发 |

## 7. 下一步

- 复杂度 **standard**：改动面中等（约 6 个 chrome 组件 + 4 个内容文件的字段级移植），但因"外科式提取 + 事实红线自查"需要谨慎，不宜盲做。
- 建议：`/devagent:dev-spec-dev reintroduce-pr29-quality --skip-requirements`，在 `feat/reintroduce-a11y-quality` 分支按 R1→R2→R3→R4 顺序 TDD 移植，每组独立 commit，逐组跑 typecheck/test/build。
- 依赖：建议先让 revert PR #30 合并（或明确以 revert 分支为基），避免基线漂移。
- 也可让我先做 R1+R2（纯 chrome/安全、最干净）给你看效果，再决定 R3/R4 文案是否照此引入。
