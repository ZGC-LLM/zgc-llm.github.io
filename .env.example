# 统一合作/加入申请入口（公开 HTTPS 分享链接，不是密钥）。
# 仅在逐项确认表单对象、匿名可用性、隐私说明和回执后配置；
# 留空时的降级/默认行为以 src/config/site.ts 的当前实现为准。
NEXT_PUBLIC_APPLICATION_URL=
# 网络安全工作组专属公开 HTTPS 表单链接；适用相同的发布前核验规则。
NEXT_PUBLIC_APPLICATION_URL_CYBERSECURITY=

# 唯一正式 canonical 域名。本地开发不应将 localhost 等值带入发布构建。
NEXT_PUBLIC_SITE_URL=https://www.zgc-llm.org.cn

# 仅供 `next dev` 使用的额外 host 白名单，逗号分隔 exact DNS hostname/IPv4。
# 例：`devbox.local,192.168.1.50`；值会 trim、转小写并去重。
# 拒绝 scheme、port、path、query、fragment、userinfo、反斜杠、wildcard 和空列表项；
# IPv4 每段必须为 0–255 且无前导零，任一非法值都使启动 fail-fast。未设置/留空时不额外放行。
NEXT_PUBLIC_DEV_ORIGIN=
